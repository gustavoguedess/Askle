<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.askle.askle">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label=""
        android:roundIcon="@drawable/logobranco"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="com.askle.askle.MainActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsFragment"
            android:label="@string/title_activity_maps" />
        <activity android:name=".TelaLogin" />
        <activity android:name=".TelaRegistro" />
        <activity android:name=".TelaLugaresSalvos" />
        <activity android:name=".TelaEscolhaInicial" />
        <activity android:name=".TelaSobre" />
        <activity android:name=".TelaInfoConsultorio" />
        <activity android:name=".TelaMarcarConsulta" />
        <activity android:name=".TelaTermos" />
        <activity android:name=".TelaConfig" />
        <activity android:name=".TelaFeedback" />
        <activity android:name=".TelaPerfil" />
        <activity android:name=".TelaConfPerfil" />
        <activity android:name=".TelaCadastroClinica" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".TelaPerfilCons" />
        <activity android:name=".TelaConfigClinica" />
        <activity android:name=".TelaEditarPerfilClinica" />
        <activity android:name=".TelaSolicitacoesClinica" />
        <activity android:name=".TelaLoginConsultorio" />
        <activity android:name=".TelaEsqueciSenha"></activity>
    </application>

</manifest>